connector = "openai"
model = "zai-org/GLM-5.2"
base_url = "https://api.tokenfactory.nebius.com/v1/"
api_key_env = "NEBIUS_API_KEY"
label = "GLM-5.2"
aliases = ["neb:zai-org/GLM-5.2"]
order = 90
run = true
note = "on this hosting it periodically stops answering altogether; runs last so a stall costs the sweep nothing"
