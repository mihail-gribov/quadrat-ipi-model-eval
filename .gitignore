__pycache__/
*.py[cod]
.venv/
.env

# your own sweeps: plain jsonl written by matrix.py. The published sweeps are the .gz files.
data/episodes-*.jsonl
data/calls-*.jsonl
data/probe_*.jsonl

# the corpus rows: fetched from Hugging Face by corpus.py, selected by data/labels_money.jsonl
data/quadrat-money.jsonl
