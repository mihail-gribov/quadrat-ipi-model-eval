# The published row was shot through an OpenAI-compatible aggregator (the alias); this config
# reaches the vendor directly through the Messages API.
connector = "anthropic"
model = "claude-haiku-4-5"
api_key_env = "ANTHROPIC_API_KEY"
label = "claude-haiku-4.5"
aliases = ["oac:anthropic/claude-haiku-4-5", "tw:anthropic/claude-haiku-4-5"]
run = true
